# Core ML
torch>=2.1.0
transformers>=4.40.0
peft>=0.10.0
trl>=0.8.6
accelerate>=0.28.0
bitsandbytes>=0.43.0
datasets>=2.18.0
sentencepiece>=0.2.0
protobuf>=3.20.0

# Orchestration (agentic graph)
langgraph>=0.2.0

# RAG — hybrid retrieval (LangChain EnsembleRetriever: BM25 + vector, RRF)
langchain>=0.2.0
langchain-community>=0.2.0
langchain-chroma>=0.1.2
langchain-huggingface>=0.0.3
rank-bm25>=0.2.2
chromadb>=0.4.24
sentence-transformers>=2.7.0

# MCP (enterprise tool layer)
mcp>=1.0

# vLLM serving (optional; ROCm wheels). Install separately if deploying the
# expert lane as an API endpoint:  pip install vllm
# vllm

# API & UI
fastapi>=0.111.0
uvicorn>=0.29.0
streamlit>=1.33.0
pydantic>=2.7.0

# AMD GPU monitoring
psutil>=5.9.8
py3nvml>=0.2.7

# Utils
python-dotenv>=1.0.1
httpx>=0.27.0
tqdm>=4.66.0
numpy>=1.26.0
pandas>=2.2.0
